@model MAMS.Models.DoctorDetails

<div class="g-col-6">
    <div class="card card-primary card-outline">
        <div class="card-body box-profile">
            <div class="text-center">
                <img class="profile-user-img img-fluid img-circle"
                     src="~/img/Doctor.png"
                     alt="User profile picture">
                <input asp-for="@Model.SuserId" hidden />
                <input asp-for="@Model.Id" hidden />
            </div>

            <h3 class="profile-username text-center">@Model.First_Name @Model.Last_Name</h3>

            <p class="text-muted text-center">@Model.Specialization</p>

            @* <ul class="list-group list-group-unbordered mb-3">
                <li class="list-group-item">
                    <b>Followers</b> <a class="float-right">1,322</a>
                </li>
                <li class="list-group-item">
                    <b>Following</b> <a class="float-right">543</a>
                </li>
                <li class="list-group-item">
                    <b>Friends</b> <a class="float-right">13,287</a>
                </li>
            </ul> *@

            <a asp-action="Availabilities" asp-route-id="@Model.Id" class="btn btn-primary btn-block"><b>Book Appointment</b></a>
        </div>
        <!-- /.card-body -->
    </div>
</div>